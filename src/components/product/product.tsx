/**
 * Must have an image
 * Must have a title
 * Must have a price
 * Must have a total
 * Must have a quantity
 * Must have a button to add to cart
 * 
 * @returns a product card
 */

import './product.css'

export const Product = () => {
    return (
        <div>

        </div>
    )
}